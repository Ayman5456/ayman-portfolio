const { defineConfig } = require("vite");
const react = require("@vitejs/plugin-react");

module.exports = defineConfig(({ command }) => ({
  plugins: [react()],
  // Use the GitHub Pages subpath only for production builds
  base: command === "build" ? "/ayman-portfolio/" : "/",
}));
